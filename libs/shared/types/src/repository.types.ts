/**
 * GitHub User's Repository Model
 *
 * @export
 * @interface Repository
 */
export interface Repository {
  id: string;
  name: string;
  description: string;
  stargazerCount: number;
  createdAt: string;
}
